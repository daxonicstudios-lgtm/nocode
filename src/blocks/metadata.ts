// Auto-generated block metadata for server-side AI matching
// Imports from JSON for fast parsing (no 15K-line TS file)

import metadataJson from "./metadata.json";

export interface BlockMetaLite {
  slug: string;
  category: string;
  tags: string[];
  style: string;
  industries: string[];
}

export const BLOCK_METADATA: BlockMetaLite[] = metadataJson as BlockMetaLite[];
